<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ONE_FINANCE.ViewModels"
             x:Class="ONE_FINANCE.Views.SettingsPage"
             x:DataType="viewmodels:SettingsViewModel"
             Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            
            <!-- Header -->
            <Label Text="⚙️ Settings" 
                   FontSize="28" 
                   FontAttributes="Bold"/>

            <!-- App Information Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                   BorderColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                   CornerRadius="10" 
                   Padding="15">
                <VerticalStackLayout Spacing="15">
                    <Label Text="App Information" 
                           FontSize="18" 
                           FontAttributes="Bold"/>
                    
                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" RowSpacing="10">
                        <Label Grid.Row="0" Grid.Column="0" 
                               Text="Version" 
                               FontAttributes="Bold"/>
                        <Label Grid.Row="0" Grid.Column="1" 
                               Text="{Binding AppVersion}" 
                               HorizontalOptions="End"/>
                        
                        <Label Grid.Row="1" Grid.Column="0" 
                               Text="Database Location" 
                               FontAttributes="Bold"/>
                        <Label Grid.Row="1" Grid.Column="1" 
                               Text="{Binding DatabasePath}" 
                               FontSize="10" 
                               LineBreakMode="TailTruncation"
                               HorizontalOptions="End"/>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Data Management Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                   BorderColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                   CornerRadius="10" 
                   Padding="15">
                <VerticalStackLayout Spacing="15">
                    <Label Text="Data Management" 
                           FontSize="18" 
                           FontAttributes="Bold"/>
                    
                    <Label Text="Your data is stored locally on this device. No cloud sync is enabled." 
                           FontSize="12" 
                           TextColor="Gray"/>
                    
                    <!-- Placeholder for future export/backup features -->
                    <Label Text="Export and backup features coming soon..." 
                           FontSize="12" 
                           TextColor="Gray" 
                           FontAttributes="Italic"/>
                </VerticalStackLayout>
            </Frame>

            <!-- About Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                   BorderColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                   CornerRadius="10" 
                   Padding="15">
                <VerticalStackLayout Spacing="10">
                    <Label Text="About" 
                           FontSize="18" 
                           FontAttributes="Bold"/>
                    
                    <Label Text="One Finance is a personal finance management application designed to help you track your income, expenses, and financial goals." 
                           FontSize="12"/>
                    
                    <Label Text="Built with .NET MAUI" 
                           FontSize="12" 
                           TextColor="Gray"/>
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
